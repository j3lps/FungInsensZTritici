#ifndef		CANOPY_DEVELOPMENT_H
#define		CANOPY_DEVELOPMENT_H

double	canopyGrowth(double t);		// Simulates the growth of the host canopy
double	canopySenescence(double t);		// Simulates the senescence of the host canopy
void	initialiseCanopy(double *pNew);

#endif // Header file is included